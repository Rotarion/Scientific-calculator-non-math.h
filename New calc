#include <iostream>
#include <bits/stdc++.h>    //
#include <iomanip>
#include <complex>          //
using namespace std;

double calculatorfunction (double a, double b, char op);
int fact(int n);
int comb(int n, int r);
int per(int n, int r);
double sinh(double n);
double cosh(double n);
double tanh(double n);
int fibonnaci (int n);
int pasca(int rows);

int main()
{
    char op = '+';
    double a, b;

    cout << "Please input the two numbers and the operator, in case of not needed the second number choose 0.\n";
    cout << "Possible operators;\n";
    cout << "+ - * / \nF for factorials.\nComb for combinations.\nPerm for permutations.\nSinh for hyperbolic sin.\nCosh for hyperbolic cosine.\nTanh for hyperbolic tangent.\nQ for quit.\n\n";
    cout << "Number 1: ";
    cin >> a;
    cout << "Number 2: ";
    cin >> b;
    cout << "Operator: ";
    cin >> op;

    while (op != 'Q')
    {
        cout << fixed;
        cout << "Result: " << calculatorfunction(a, b, op) << endl << endl;

        cout << "Please input the two numbers and the operator, in case of not needed the second number choose 0.\n";
        cout << "Possible operators;\n";
        cout << "+ - * / \nF for factorials.\nComb for combinations.\nPerm for permutations.\nSinh for hyperbolic sin.\nCosh for hyperbolic cosine.\nTanh for hyperbolic tangent.\nQ for quit.\n\n";
        cout << "Number 1: ";
        cin >> a;
        cout << "Number 2: ";
        cin >> b;
        cout << "Operator: ";
        cin >> op;
    }
    return 0;
}

double calculatorfunction (double a, double b, char op)
{
    double result = 0.0;

    if (op == '+')
    {
        result = a + b;
    }
    else if (op == '-')
    {
        result = a - b;
    }
    else if (op == '*')
    {
        result = a * b;
    }
    else if (op == '/')
    {
        result = a / b;
    }
    else if (op == '^')
    {
        result = pow(a,b);
    }
    else if (op == 'F')
    {
        b = 0;
        result = fact(a);
    }
    else if (op == 'Comb')
    {
        result = comb(a,b);
    }
    else if (op == 'Perm')
    {
        result = per(a,b);
    }
    else if (op == 'Sinh')
    {
        b = 0;
        result = sinh(a);
    }
    else if (op == 'Cosh')
    {
        b = 0;
        result = cosh(a);
    }
    else if (op == 'Tanh')
    {
        b = 0;
        result = tanh(a);
    }
    else
    {
        cout << "Bad input please go back to inputs and select a proper input." << endl;
        return 0;
    }
    return result;
}

int fact(int n)
{
    if (n == 0 || n == 1)
    {
        return 1;
    }
    else if (n < 0)
    {
        cout << "Sorry factorials can only be used on positive integers." << endl;
    }
    else
    {
        return n * fact(n-1);
    }
}

int comb(int n, int r) {
   return fact(n) / (fact(r) * fact(n-r)) ;
}

int per(int n, int r) {             //formula for the permutation
    return fact(n) / fact(n-r);
}

double sinh(double n)
{
    double e= 2.71828;
    return  (pow(e,n)-pow(e,-n))/2;
}

double cosh(double n)
{
    double e= 2.71828;
    return  (pow(e,n)-pow(e,-n))/2;
}

double tanh(double n)
{
    double e= 2.71828;
    return  (pow(e,n)-pow(e,-n))/(pow(e,n)+pow(e,-n));
}

int fibonnaci (int n)
{
   int t1 = 0, t2 = 1, nextTerm = 0;

    for (int i = 1; i <= n; ++i)
    {
        // Prints the first two terms.
        if(i == 1)
        {
            cout << " " << t1;
            continue;
        }
        if(i == 2)
        {
            cout << t2 << " ";
            continue;
        }
        nextTerm = t1 + t2;
        t1 = t2;
        t2 = nextTerm;

         cout << nextTerm << " ";
    }
}

int pasca(int rows)
{
    if (rows >=21)
    {
        cout << "Sorry memorry error the calculator can only run 20 rows." << endl;
    }
    else if (rows <= 0)
    {
        int coef = 1;

        for(int i = 0; i < rows; i++)
        {
            for(int space = 1; space <= rows-i; space++)
                cout <<"  ";

            for(int j = 0; j <= i; j++)
            {
                if (j == 0 || i == 0)
                    coef = 1;
                else
                    coef = coef*(i-j+1)/j;

                cout << coef << "   ";
            }
            cout << endl;
        }
    }
    else{
        cout << "Please enter a positive integer.";
    }
}
